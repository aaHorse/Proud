<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.horse.proud">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:name="com.horse.proud.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="false"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:allowBackup="false"
        tools:targetApi="n"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name="com.horse.proud.ui.common.HolderActivity"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout"/>

        <activity android:name="com.horse.proud.ui.home.MainActivity">

        </activity>

        <activity android:name="com.horse.proud.ui.init.ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com.horse.proud.ui.login.ui.LoginActivity">

        </activity>

        <meta-data
            android:name="APP_CHANNEL"
            android:value="opensource"/>
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="5c18feb8f1f5568cb70001f9"/>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="opensource"/>

    </application>

</manifest>